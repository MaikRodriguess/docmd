version: "3.8"

services:
  web:
    build: .
    image: docmd:latest
    ports:
      - "80:5000"
    env_file:
      - .env
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
    driver: local
